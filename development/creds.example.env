# Copy this to development/creds.env and change the token. creds.env is ignored
# by git.
#
# The headless container cannot show you a token in a dock widget, so both
# containers read it from here instead.
QGIS_MCP_TOKEN=change-me-a-long-random-string
